# Benchmark workspace

This directory stores declared cohort manifests and derived audit reports. Raw
PDB coordinates are retrieved from their source URLs by script and are not
committed to the repository.

The initial pilot is a *candidate inventory*, not a completed benchmark. A
candidate becomes selected only after it is checked against the eligibility and
independence criteria in `docs/STUDY-DESIGN.md`.
